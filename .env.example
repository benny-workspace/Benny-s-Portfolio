# APP_URL: The public URL where this site is hosted.
# Used for canonical/OG tags, sitemap.xml, and any self-referential links.
APP_URL="https://ceobennyco.com"

# --- Optional: lead capture (see Phase 1.2 / CHANGELOG) ---
# If/when the newsletter or a contact form is wired to a backend, point it here.
# Client code should only ever hold a PUBLIC endpoint (a serverless function or a
# form service). Never put a private API key (email provider, Supabase service
# role, Gemini, etc.) in client-bundled env vars — those belong server-side only.
# VITE_LEAD_CAPTURE_ENDPOINT=""
